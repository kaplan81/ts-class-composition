{
  "name": "ts-class-composition-repo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-decorators": "npm run clean-dist &&  npx tsc --project ./projects/decorators && node dist/component-with-decorators.js",
    "build-mixins": "npm run clean-dist && npx tsc --project ./projects/mixins && node dist/component-with-mixins.js",
    "clean-dist": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tslib": "^2.6.2",
    "typescript": "^5.3.3"
  },
  "type": "commonjs",
  "dependencies": {
    "rxjs": "^7.8.1"
  }
}
